<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="tyshop.mapper.BuyerReceiverMapper">

    <resultMap id="BuyerReceiverMap" type="tyshop.model.BuyerReceiverModel">
        <id column="id" property="id"/>
        <result column="buyer_id" property="bugerId"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="area_id" property="areaId"/>
        <result column="area_tree_path" property="areaTreePath"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="phone" property="phone"/>
        <result column="address" property="address"/>
        <result column="is_default" property="isDefault"/>
        <result column="created" property="created"/>
        <result column="updated" property="updated"/>
        <result column="active" property="active"/>
    </resultMap>

    <insert id="insert" parameterType="tyshop.model.BuyerReceiverModel">
         <![CDATA[
            insert into t_buyer_receiver (
            seller_id, receiver_name, area_id, area_tree_path, province,city,
            district, phone, address, is_default,
            created,
            updated,
            active
            ) VALUES (
            #{bugerId},#{receiverName},#{areaId},#{areaTreePath},#{province},#{city},#{district},
            #{phone},#{address},#{is_default},
            #{create},
            #{updated},
            #{active}
            )
        ]]>
    </insert>

    <select id="listOrderBySellerId" resultMap="BuyerReceiverMap" parameterType="String">
        <![CDATA[
            select
            seller_id, receiver_name, area_id, area_tree_path,province,
            city, district, phone, point, address, is_default,
            created,
            updated,
            active
            from t_buyer_receiver where buyer_id = #{bugerId}
        ]]>
    </select>


</mapper>